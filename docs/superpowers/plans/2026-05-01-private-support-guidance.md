# 私域联系方式引导实施计划

## 计划

- 在创作台表单底部增加专属服务面板。
- 使用 `VITE_PRIVATE_SUPPORT_QQ` 和 `VITE_PRIVATE_SUPPORT_WECHAT` 控制展示内容。
- 补充面板样式，保持与现有工作台视觉一致。
- 增加前端测试覆盖专属服务入口。
- 更新 README 的部署环境变量说明。

## 验证

- `npm test -- --run WorkspacePage`
- `npm run build`
